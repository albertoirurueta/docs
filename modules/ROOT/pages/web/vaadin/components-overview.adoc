= Components Overview
:description: How Vaadin Flow components are structured -- the Component base class and the Has* mixin interfaces, adding and removing children, enabled and visible state and the security consequence of server-side setVisible(false), a grouped tour of the 40-plus built-in components, and which components need a commercial subscription.
:keywords: Vaadin, Flow, Component, HasComponents, HasSize, HasStyle, HasValue, HasEnabled, Focusable, add, remove, removeAll, getChildren, setEnabled, setVisible, built-in components, Grid, ComboBox, Dialog, layouts, Pro components, commercial, Grid Pro, Charts, CRUD, Spreadsheet, Rich Text Editor, Map, Dashboard, Cookie Consent

include::partial$vaadin-disclaimer.adoc[]

Every Vaadin Flow UI is a server-side tree of `Component` objects, each paired with one client-side element and
each mixing in a small set of capability interfaces. This page maps that model and tours the built-in
component set; see https://vaadin.com/docs/latest/components[Components] for the full catalog and
https://vaadin.com/docs/latest/building-apps/ui-basics/compose[Compose with Components] for how to break a
view into a hierarchy.

== The Component base class and the Has* mixins

`com.vaadin.flow.component.Component` is the superclass of every UI component. It wraps a single root
`Element`, and exposes lifecycle and identity methods -- `getElement()`, `getParent()`, `getChildren()`,
`setId()`, `setVisible()`, and attach/detach events. Beyond that, each component implements only the
*mixin interfaces* for the capabilities it actually has, so the API a component offers is visible from its
`implements` clause. See https://vaadin.com/docs/latest/flow/component-internals/mixins[Using Vaadin Mixin
Interfaces].

[cols="1,3",options="header"]
|===
| Interface | What it contributes

| `Component` | Base class: one root `Element`, `getElement()`, `getParent()`, `getChildren()` (a
  `Stream<Component>`), `setId()`, `setVisible()`, `addAttachListener` / `addDetachListener`.

| `HasComponents` | `add(Component\...)`, `remove(Component\...)`, `removeAll()`, `addComponentAsFirst`,
  `addComponentAtIndex` -- implemented by containers such as `Div`, `VerticalLayout` and `FormLayout`.

| `HasSize` | Dimension setters: `setWidth`, `setHeight`, `setMinWidth` / `setMaxWidth`, `setSizeFull`,
  `setSizeUndefined`, plus the matching getters.

| `HasStyle` | CSS class and inline-style API: `addClassName`, `removeClassName`, `setClassName`,
  `getClassNames()`, and `getStyle()` for individual inline properties.

| `HasValue<E, V>` | The field contract: `getValue` / `setValue`, `isEmpty`, `clear`,
  `addValueChangeListener`, `setReadOnly`, `setRequiredIndicatorVisible`. This is what
  xref:web/vaadin/data-binding.adoc[`Binder`] binds against.

| `HasEnabled` | `setEnabled(boolean)` / `isEnabled()`. A disabled component is greyed out *and* the server
  drops events and property updates coming from it -- see
  https://vaadin.com/docs/latest/flow/component-internals/enabled-state[Component Enabled State].

| `Focusable<T>` | Keyboard focus: `focus()`, `blur()`, `setTabIndex()`, `addFocusListener`,
  `addBlurListener`.
|===

Related mixins you will meet include `HasText`, `HasLabel`, `HasHelper`, `HasTheme`, `HasTooltip` and
`HasValidation`; the mixins page above lists them all. Because these interfaces are additive, a single field
carries several at once:

[source,java]
----
// TextField implements Component, HasSize, HasStyle, HasValue<..., String>, HasEnabled, Focusable<TextField>, ...
TextField name = new TextField("Name");

name.setWidth("20em");                       // HasSize
name.addClassName("highlight");              // HasStyle
name.getStyle().set("margin-top", "0.5em"); // HasStyle
name.setValue("Ada");                        // HasValue
name.addValueChangeListener(e -> System.out.println(e.getValue()));
name.setEnabled(false);                      // HasEnabled
name.focus();                                // Focusable
----

Custom components acquire the same API by extending `Component` (or `Composite`) and implementing whichever
mixins apply -- covered in xref:web/vaadin/custom-components.adoc[].

== Adding and removing children

Any component that implements `HasComponents` manages its children through the same four methods, regardless
of which layout it is. Structural changes take effect on the next server round trip.

[source,java]
----
VerticalLayout layout = new VerticalLayout();   // implements HasComponents
Span a = new Span("A");
Span b = new Span("B");

layout.add(a, b);                       // append one or more children
layout.addComponentAsFirst(new Span("header"));
layout.addComponentAtIndex(1, new Span("inserted"));

layout.remove(a);                       // detach a specific child
layout.removeAll();                     // detach every child

List<Component> children = layout.getChildren().toList();  // getChildren() returns a Stream<Component>
----

`getChildren()` returns only the *direct* children as a `Stream`; walk it recursively for the whole subtree.
`remove` on a component that is not a child throws `IllegalArgumentException`, while `removeAll` is always
safe. Adding a component that already has a parent moves it -- it is detached from the old parent first.

== Enabled and visible state

Two independent flags control whether a component is live.

[source,java]
----
Button save = new Button("Save");

save.setEnabled(false);   // rendered but greyed out; the server ignores clicks and value changes from it
save.setVisible(false);   // not rendered in the browser at all
----

`setEnabled(false)` keeps the element in the DOM but tells the server to reject any event or property update
originating from it (and from its descendants). `setVisible(false)` goes further: before first render the
element is never created, and after render Flow marks it `hidden`, stops sending it updates, and *ignores
RPCs that target it*.

[IMPORTANT]
====
*Security:* server-side `setVisible(false)` -- or simply never adding a component -- means the component's
markup and the data bound into it are *never sent to the browser*, and the server will not act on requests
against it. This is fundamentally different from hiding an element with CSS (`display: none` /
`visibility: hidden`), where the value still sits in the DOM and is trivially readable from dev tools or a
crafted request. A field that must not reach an unauthorized user should be removed or made invisible on the
server, not merely styled away. See
https://vaadin.com/docs/latest/flow/component-internals/visibility[Component Visibility] and the
https://vaadin.com/docs/latest/flow/security[Flow security] documentation, whose architecture overview notes
that Vaadin "denies actions to components that aren't currently visible on the screen."
====

== The built-in components: a grouped tour

Vaadin ships more than 40 components. They fall into a few groups, each with its own dedicated page in this
section; the https://vaadin.com/docs/latest/components[Components] catalog has a live example for every one.

[cols="1,2,1",options="header"]
|===
| Group | Components (selection) | Dedicated page

| Input fields
| `TextField`, `TextArea`, `EmailField`, `PasswordField`, `NumberField`, `IntegerField`, `BigDecimalField`,
  `Checkbox`, `CheckboxGroup`, `RadioButtonGroup`, `ComboBox`, `MultiSelectComboBox`, `Select`, `ListBox`,
  `DatePicker`, `TimePicker`, `DateTimePicker`, `Upload`, `CustomField`
| xref:web/vaadin/input-components.adoc[]

| Buttons & menus
| `Button`, `MenuBar`, `ContextMenu`, `Tabs` / `TabSheet`, `SideNav`, `Breadcrumbs`
| xref:web/vaadin/interaction-and-overlays.adoc[]

| Data visualization
| `Grid`, `TreeGrid`, `GridPro`, `VirtualList`, `Charts`, `Spreadsheet`, `ProgressBar`, `Avatar` /
  `AvatarGroup`, `Badge`, `MessageList`
| xref:web/vaadin/grid.adoc[]

| Layouts
| `VerticalLayout`, `HorizontalLayout`, `FlexLayout`, `FormLayout`, `SplitLayout`, `Scroller`, `AppLayout`,
  `MasterDetailLayout`, `Accordion`, `Details`, `Card`, `Dashboard`
| xref:web/vaadin/layouts.adoc[]

| Overlays & feedback
| `Dialog`, `ConfirmDialog`, `Popover`, `Notification`, `Tooltip`, `LoginOverlay`
| xref:web/vaadin/interaction-and-overlays.adoc[]
|===

*Input fields* all implement `HasValue`, so they plug straight into a `Binder`
(xref:web/vaadin/data-binding.adoc[]). The range runs from
https://vaadin.com/docs/latest/components/text-field[`TextField`] and
https://vaadin.com/docs/latest/components/combo-box[`ComboBox`] to the lazy-loading
https://vaadin.com/docs/latest/components/multi-select-combo-box[`MultiSelectComboBox`] and the
https://vaadin.com/docs/latest/components/date-picker[`DatePicker`] family;
https://vaadin.com/docs/latest/components/upload[`Upload`] handles file transfer and
https://vaadin.com/docs/latest/components/custom-field[`CustomField`] composes several inputs into one value.

*Buttons and menus* drive actions. https://vaadin.com/docs/latest/components/button[`Button`] carries
variants and icons, https://vaadin.com/docs/latest/components/menu-bar[`MenuBar`] and
https://vaadin.com/docs/latest/components/context-menu[`ContextMenu`] build nested menus, and
https://vaadin.com/docs/latest/components/tabs[`Tabs`] plus
https://vaadin.com/docs/latest/components/side-nav[`SideNav`] structure navigation.

*Data visualization* centers on https://vaadin.com/docs/latest/components/grid[`Grid`] -- a lazy-loading,
sortable, editable data table (in Vaadin 7 this role was filled by `Table`) -- with
https://vaadin.com/docs/latest/components/tree-grid[`TreeGrid`] for hierarchies and
https://vaadin.com/docs/latest/components/virtual-list[`VirtualList`] for custom row rendering. See
xref:web/vaadin/grid.adoc[] for data providers and lazy loading.

*Layouts* arrange components without hand-written CSS:
https://vaadin.com/docs/latest/components/vertical-layout[`VerticalLayout`] /
https://vaadin.com/docs/latest/components/horizontal-layout[`HorizontalLayout`] wrap flexbox,
https://vaadin.com/docs/latest/components/form-layout[`FormLayout`] does responsive label grids, and
https://vaadin.com/docs/latest/components/app-layout[`AppLayout`] provides the application shell. Details in
xref:web/vaadin/layouts.adoc[].

*Overlays* open above the page: https://vaadin.com/docs/latest/components/dialog[`Dialog`] and
https://vaadin.com/docs/latest/components/confirm-dialog[`ConfirmDialog`] for modal content,
https://vaadin.com/docs/latest/components/popover[`Popover`] and
https://vaadin.com/docs/latest/components/tooltip[`Tooltip`] for anchored hints, and
https://vaadin.com/docs/latest/components/notification[`Notification`] for transient messages. See
xref:web/vaadin/interaction-and-overlays.adoc[].

[NOTE]
====
*In Hilla / React:* the same components ship as `@vaadin/react-components` and compose with JSX and hooks
instead of `add()` / `remove()`; child structure is expressed as nested elements and state, not method calls.
The visible/enabled semantics and the commercial licensing below are identical.
====

== Pro (commercial) components

Most components are free and open source. The following require an active commercial Vaadin subscription;
using any of them adds a license check at build and production start-up (see
https://vaadin.com/docs/latest/flow/configuration/licenses[License Validation]). For an at-a-glance
comparison with third-party UI kits, see xref:web/vaadin/ui-component-libraries.adoc[].

* https://vaadin.com/docs/latest/components/grid-pro[Grid Pro] -- inline cell editing on top of `Grid`.
* https://vaadin.com/docs/latest/components/crud[CRUD] -- a ready-made list-plus-editor for one dataset.
* https://vaadin.com/docs/latest/components/charts[Charts] -- interactive chart types (line, bar, pie, gauge, ...).
* https://vaadin.com/docs/latest/components/spreadsheet[Spreadsheet] -- an Excel-compatible spreadsheet surface.
* https://vaadin.com/docs/latest/components/rich-text-editor[Rich Text Editor] -- WYSIWYG editing producing HTML or Delta.
* https://vaadin.com/docs/latest/components/map[Map] -- an OpenLayers-based map (production use needs a paid tile service).
* https://vaadin.com/docs/latest/components/dashboard[Dashboard] -- a draggable, resizable widget board.
* https://vaadin.com/docs/latest/components/confirm-dialog[Confirm Dialog] -- a ready-made confirm / cancel /
  reject modal (`vaadin-confirm-dialog`).
* https://vaadin.com/docs/latest/components/cookie-consent[Cookie Consent] -- a GDPR/CCPA consent banner.

Free alternatives cover many cases -- `Grid` instead of Grid Pro, a hand-built form over a `Grid` instead of
CRUD (xref:web/vaadin/forms-crud-and-master-detail.adoc[]), and a plain `TextArea` instead of Rich Text
Editor.

== See also

* xref:web/vaadin/input-components.adoc[] -- every `HasValue` field and its binding.
* xref:web/vaadin/layouts.adoc[] -- flexbox, form and application layouts.
* xref:web/vaadin/grid.adoc[] -- `Grid`, data providers and lazy loading.
* xref:web/vaadin/interaction-and-overlays.adoc[] -- buttons, menus, dialogs and notifications.
* xref:web/vaadin/custom-components.adoc[] -- building your own component from `Component` / `Composite`.
* xref:web/vaadin/theming-and-styling.adoc[] -- `HasStyle`, Lumo and CSS.
* xref:web/vaadin/data-binding.adoc[] -- connecting fields to business objects with `Binder`.
* xref:web/vaadin/ui-component-libraries.adoc[] -- Vaadin's set versus other Java and web UI kits.
