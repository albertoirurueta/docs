= Routing
:description: Client-side routing in Vue with Vue Router -- creating the router, RouterView and RouterLink, route records, dynamic segments and params, nested and named routes, redirects and catch-all, programmatic navigation, navigation guards and the resolution flow, lazy loading, and scroll behavior.
:keywords: Vue Router, createRouter, createWebHistory, createWebHashHistory, RouterView, RouterLink, route records, dynamic segments, route params, nested routes, named routes, named views, redirect, alias, catch-all, navigation guards, beforeEach, lazy route, scrollBehavior, useRoute, useRouter

include::partial$vue-disclaimer.adoc[]

Vue's official client-side router is https://router.vuejs.org/[Vue Router]: it maps the URL to a tree of
components, keeps the address bar in sync, and provides links, params, guards, and history control. The Vue
guide introduces it at https://vuejs.org/guide/scaling-up/routing.html[Routing]; everything below is in the
Vue Router docs.

== Creating the router

Build a router instance with `createRouter`, choosing a *history* implementation, then install it with
`app.use(router)`:

[source,js]
----
import { createRouter, createWebHistory } from 'vue-router'
import Home from './views/Home.vue'

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    { path: '/', component: Home },
    { path: '/about', component: () => import('./views/About.vue') },
  ],
})
----

[source,js]
----
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

createApp(App).use(router).mount('#app')
----

`createWebHistory` uses the HTML5 History API and needs a server fallback that serves `index.html` for
unknown paths. `createWebHashHistory` keeps the route after a `#`, so it works on any static host with no
server config. `createMemoryHistory` has no address bar and is for SSR and tests. See
https://router.vuejs.org/guide/essentials/history-mode.html[History modes].

== `<RouterView>` and `<RouterLink>`

`<RouterView>` is the outlet where the matched component renders; `<RouterLink>` renders an `<a>` that
navigates without a reload and adds active classes:

[source,vue]
----
<template>
  <nav>
    <RouterLink to="/">Home</RouterLink>
    <RouterLink :to="{ name: 'user', params: { id: 42 } }">Profile</RouterLink>
  </nav>
  <RouterView />
</template>
----

== Route records

Each entry in `routes` is a *route record*: at minimum `\{ path, component }`. A colon marks a *dynamic
segment*, whose value appears on `route.params`:

[source,js]
----
const routes = [
  { path: '/users/:id', component: UserProfile },
  { path: '/files/:pathMatch(.*)*', component: FileBrowser }, // custom regex + repeatable
]
----

To keep the component decoupled from the router, set `props: true` so params are passed as component props
instead of read from `useRoute()`. `props` can also be an object (static props) or a function
`route => (\{ ... })` (derive props, including from the query string):

[source,js]
----
{ path: '/users/:id', component: UserProfile, props: true },
{ path: '/search', component: Results, props: route => ({ q: route.query.q }) },
----

See https://router.vuejs.org/guide/essentials/passing-props.html[Passing Props to Route Components].

== Nested routes

A record's `children` render into a `<RouterView>` *inside* that record's component, so a URL selects a
chain of records and each renders into the next outlet:

[source,js]
----
const routes = [
  {
    path: '/users/:id',
    component: UserLayout,
    children: [
      { path: '', name: 'user', component: UserHome },
      { path: 'posts', component: UserPosts },
      { path: 'settings', component: UserSettings },
    ],
  },
]
----

image::vue-router-outlets.svg["A URL matched to a chain of parent and child route records, each rendering into a nested RouterView outlet",width=720,role=text-center]

== Named routes and named views

Give a record a `name` to navigate by identity instead of by URL string --
`router.push(\{ name: 'user', params: \{ id: 42 } })` -- which survives path refactors. To render *several*
outlets at one level, give each `<RouterView name="...">` a name and supply a `components` map (plural) on
the record:

[source,js]
----
{
  path: '/dashboard',
  components: { default: DashMain, sidebar: DashSidebar },
}
----

See https://router.vuejs.org/guide/essentials/named-routes.html[Named Routes] and
https://router.vuejs.org/guide/essentials/named-views.html[Named Views].

== Redirect, alias, and catch-all

* `redirect` sends one path to another: `\{ path: '/home', redirect: '/' }`, or a function of the target
  route.
* `alias` lets one record answer to several paths: `\{ path: '/', component: Home, alias: '/home' }`.
* A `/:pathMatch(.*)*` record at the end matches anything unmatched -- the 404 page:

[source,js]
----
{ path: '/:pathMatch(.*)*', name: 'not-found', component: NotFound }
----

https://router.vuejs.org/guide/essentials/dynamic-matching.html#Catch-all-404-Not-found-Route[Catch-all
routes] explains why the `pathMatch` param name matters when you navigate to it programmatically.

== Programmatic navigation

`useRouter()` returns the instance; push, replace, or move through history:

[source,vue]
----
<script setup>
import { useRouter } from 'vue-router'

const router = useRouter()

function save() {
  // ...persist...
  router.push({ name: 'user', params: { id: 42 } }) // new history entry
  // router.replace(...)  -> no new entry
  // router.go(-1)        -> like the back button
}
</script>
----

== Navigation guards and the resolution flow

Guards can redirect (`return \{ name: 'login' }`), cancel (`return false`), or allow (`return true` or
nothing) a navigation. They run in a fixed order
(https://router.vuejs.org/guide/advanced/navigation-guards.html[Navigation Guards]):

. `router.beforeEach` -- global, e.g. an auth check.
. `beforeRouteLeave` in components being left.
. `beforeEnter` on the entering route record.
. `beforeRouteEnter` in entering components (no `this` yet; use its callback).
. `router.beforeResolve` -- global, after in-component guards and async components resolve.
. Navigation is confirmed; `router.afterEach` runs (cannot cancel -- use it for analytics and titles).

[source,js]
----
router.beforeEach((to) => {
  if (to.meta.requiresAuth && !isLoggedIn()) {
    return { name: 'login', query: { redirect: to.fullPath } }
  }
})
----

[source,vue]
----
<script setup>
import { onBeforeRouteLeave } from 'vue-router'

onBeforeRouteLeave(() => {
  if (formDirty.value) return window.confirm('Discard unsaved changes?')
})
</script>
----

== Lazy-loaded route components

Point `component` at a dynamic `import()` so the bundler splits that view into its own chunk, fetched on
first visit:

[source,js]
----
{ path: '/reports', component: () => import('./views/Reports.vue') }
----

See https://router.vuejs.org/guide/advanced/lazy-loading.html[Lazy Loading Routes]; it pairs well with
xref:web/vue/async-components.adoc[].

== Scroll behavior

With HTML5 history, supply `scrollBehavior` to control the viewport on navigation -- reset to top, restore
the saved position on back/forward, or scroll to an anchor:

[source,js]
----
const router = createRouter({
  history: createWebHistory(),
  routes,
  scrollBehavior(to, from, savedPosition) {
    if (savedPosition) return savedPosition
    if (to.hash) return { el: to.hash, behavior: 'smooth' }
    return { top: 0 }
  },
})
----

== `useRoute()` and `useRouter()`

In `<script setup>`, `useRoute()` returns a reactive object describing the *current* route (`params`,
`query`, `hash`, `meta`, `name`), and `useRouter()` returns the router instance for navigation. Do not
destructure `useRoute()` -- watch a getter instead so param changes on the same component are observed:

[source,vue]
----
<script setup>
import { watch } from 'vue'
import { useRoute } from 'vue-router'

const route = useRoute()
watch(() => route.params.id, (id) => load(id), { immediate: true })
</script>
----

== See also

* https://router.vuejs.org/[Vue Router documentation] -- the complete API and guide.
* xref:web/vue/state-management.adoc[] -- Pinia stores read `useRoute()` and the Router integrates with
  Vue DevTools.
* xref:web/vue/server-side-rendering.adoc[] -- `createMemoryHistory` and per-request router instances.
* xref:web/react/routing.adoc[] and xref:web/angular/routing.adoc[] -- the equivalent routers elsewhere.
