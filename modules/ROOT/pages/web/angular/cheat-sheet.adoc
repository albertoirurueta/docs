= Angular Cheat Sheet
:description: A single-page, printable modern Angular quick reference -- the ng CLI, bootstrapApplication and ApplicationConfig, a standalone component, the template binding table, the built-in control flow and defer, signal inputs and outputs, signal, computed and effect, dependency injection, RxJS interop, the HTTP client and a functional interceptor, the router and a functional guard, a typed reactive form, the lifecycle-hook order, and a TestBed skeleton -- with links to every detailed page in this section.
:keywords: Angular cheat sheet, quick reference, printable Angular reference, ng CLI, bootstrapApplication, standalone component, control flow, defer, signals, computed, effect, inject, provideHttpClient, HttpInterceptorFn, provideRouter, CanActivateFn, typed reactive forms, TestBed

include::partial$angular-disclaimer.adoc[]

A single-page, colour-coded summary of modern standalone Angular -- the `ng` CLI, `bootstrapApplication`
with `ApplicationConfig`, a standalone component skeleton, the template binding table, the built-in `@if` /
`@for` / `@switch` control flow and `@defer`, signal `input()` / `output()` / `model()`, `signal` /
`computed` / `effect`, dependency injection with `inject()` and `InjectionToken`, RxJS interop with the
`async` pipe and `toSignal`, `provideHttpClient` with `HttpClient` and a functional interceptor,
`provideRouter` with `routerLink` and a functional guard, a typed reactive `FormGroup` with `Validators`,
the lifecycle-hook order, and a `TestBed` skeleton -- handy for quick memorization or as a printable desk
reference.

For the full explanation, every option, and additional examples, see the dedicated pages:

*Getting started* --
xref:web/angular/getting-started.adoc[Getting Started with Angular] and
xref:web/angular/typescript-essentials.adoc[TypeScript Essentials].

*Components & templates* --
xref:web/angular/components.adoc[Components],
xref:web/angular/templates-and-binding.adoc[Templates and Data Binding],
xref:web/angular/control-flow-and-defer.adoc[Built-in Control Flow and @defer],
xref:web/angular/directives.adoc[Directives], and
xref:web/angular/pipes.adoc[Pipes].

*Communication & lifecycle* --
xref:web/angular/component-communication.adoc[Component Communication] and
xref:web/angular/lifecycle-and-change-detection.adoc[Lifecycle and Change Detection].

*Reactivity* --
xref:web/angular/signals.adoc[Signals and Reactivity] and
xref:web/angular/rxjs-and-async.adoc[Reactive Patterns with RxJS].

*Services, data & routing* --
xref:web/angular/dependency-injection.adoc[Services and Dependency Injection],
xref:web/angular/http-client.adoc[The HTTP Client], and
xref:web/angular/routing.adoc[Routing and Navigation].

*Forms & errors* --
xref:web/angular/forms.adoc[Forms] and
xref:web/angular/error-handling.adoc[Error Handling].

*Quality & delivery* --
xref:web/angular/testing.adoc[Unit Testing] and
xref:web/angular/production-and-performance.adoc[Production Build and Performance].

*UI & ecosystem* --
xref:web/angular/styling-and-ui-libraries.adoc[Styling and UI Component Libraries],
xref:web/angular/angular-material.adoc[Angular Material and the CDK], and
xref:web/angular/ecosystem-and-migration.adoc[Ecosystem and the NgModule Migration].

xref:attachment$angular-cheat-sheet.pdf[Download the Angular Cheat Sheet (PDF)]
