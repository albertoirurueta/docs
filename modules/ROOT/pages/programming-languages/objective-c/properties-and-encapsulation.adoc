= Properties and Encapsulation
:description: The Objective-C @property directive and its full attribute table -- strong, weak, copy, assign, unsafe_unretained, atomic, nonatomic, readonly, readwrite, getter=, setter=, class and nullability -- plus autosynthesis, @synthesize and @dynamic, backing ivars, dot syntax, KVC-compliant accessor naming and the readonly-public/readwrite-private idiom.
:keywords: Objective-C, property, strong, weak, copy, assign, unsafe_unretained, atomic, nonatomic, readonly, readwrite, getter, setter, class property, nullable, synthesize, dynamic, backing ivar, dot syntax, KVC, class extension

include::partial$objective-c-disclaimer.adoc[]

A `@property` declares a *contract for accessing a value*: a getter, usually a setter, a memory-management
policy and an atomicity guarantee. The compiler generates the accessors and the backing instance variable for
you. Properties replaced hand-written accessor pairs in Objective-C 2.0 and are now the default way to express
any piece of object state.

== Declaring a Property

[source,objc]
----
@interface Person : NSObject
@property (nonatomic, copy)   NSString  *name;
@property (nonatomic, assign) NSInteger  age;
@property (nonatomic, weak)   id<PersonDelegate> delegate;
@property (nonatomic, readonly) NSString *fullDescription;
@end
----

Each line generates, by default:

* a getter named after the property (`name`, `age`),
* a setter named `set` + capitalised name (`setName:`, `setAge:`), unless `readonly`,
* a backing ivar named with a leading underscore (`_name`, `_age`).

== The Attribute Table

Attributes come in four independent groups. Pick one from each group that applies; the order inside the
parentheses does not matter.

=== Memory Management

[cols="1,3",options="header"]
|===
| Attribute | Meaning

| `strong`
| The property owns the object: setting it retains the new value and releases the old. **The default for
  object types under ARC.**

| `weak`
| A non-owning reference that is **automatically set to `nil`** when the object is deallocated. The correct
  choice for delegates, parent back-pointers, and anything that must not create a retain cycle.

| `copy`
| The setter stores a `copy` of the value rather than the value itself. Essential for any property whose type
  has a mutable subclass -- `NSString`, `NSArray`, `NSDictionary`, `NSSet`, `NSData` -- and for blocks.

| `assign`
| Plain assignment with no memory management. Correct for scalars (`NSInteger`, `BOOL`, `CGFloat`) and C
  structs. **The default for non-object types.**

| `unsafe_unretained`
| A non-owning object reference that is *not* zeroed on deallocation -- it becomes a dangling pointer. Use
  only for the rare class that does not support weak references (a few Core Foundation-backed classes), or for
  deliberate performance work.

| `retain`
| A synonym for `strong`, kept for historical reasons. Prefer `strong`.
|===

The `copy`-for-`NSString` rule is worth spelling out, because the bug it prevents is subtle:

[source,objc]
----
@property (nonatomic, copy)   NSString *nameCopied;
@property (nonatomic, strong) NSString *nameStrong;

NSMutableString *m = [NSMutableString stringWithString:@"Ada"];
person.nameCopied = m;
person.nameStrong = m;

[m appendString:@" Lovelace"];

person.nameCopied;   // @"Ada"           -- an immutable snapshot was taken
person.nameStrong;   // @"Ada Lovelace"  -- the caller mutated your object's state!
----

`copy` on an already-immutable value is essentially free: `NSString`'s `copy` returns `self` with a retain.

=== Atomicity

[cols="1,3",options="header"]
|===
| Attribute | Meaning

| `atomic`
| The default. The generated accessors use a lock so that a get or a set is never interleaved with another
  get or set -- you can never read a half-written pointer.

| `nonatomic`
| No locking. Faster, and what virtually all application code uses.
|===

**`atomic` is not thread safety.** It guarantees only that an individual accessor call is indivisible. It says
nothing about the consistency of two related properties, and this classic sequence is still a race under
`atomic`:

[source,objc]
----
if (self.items.count > 0) {         // atomic read
    id first = self.items[0];       // another thread may have emptied it in between
}
----

Real thread safety requires a lock, a serial queue or an immutable design -- see
xref:programming-languages/objective-c/concurrency.adoc[Concurrency]. Because `atomic` costs performance
without buying safety, `nonatomic` is the conventional default; write it explicitly on every property.

=== Access

[cols="1,3",options="header"]
|===
| Attribute | Meaning

| `readwrite` | Getter and setter are generated. The default.
| `readonly` | Only a getter is generated. Callers cannot assign.
| `getter=name` | Rename the getter. Conventional for Booleans: `getter=isEnabled`.
| `setter=name:` | Rename the setter. Rare.
| `class` | Declares a **class property** -- accessors on the class object rather than instances. No ivar is
  synthesised; you must implement the accessors yourself.
| `nullable` / `nonnull` / `null_resettable` | Nullability, covered in
  xref:programming-languages/objective-c/lightweight-generics-and-nullability.adoc[Lightweight Generics and
  Nullability].
|===

[source,objc]
----
@property (nonatomic, assign, getter=isEnabled) BOOL enabled;
// generates -isEnabled and -setEnabled:

@property (class, nonatomic, readonly) Person *defaultPerson;
// [Person defaultPerson] -- implement +defaultPerson yourself
----

The `getter=is…` form is not decoration: Cocoa's key-value coding and bindings look for exactly this naming,
and `if (view.isHidden)` reads far better than `if (view.hidden)`.

== Autosynthesis, `@synthesize` and `@dynamic`

Modern Clang synthesises accessors and the backing ivar automatically -- `@synthesize` is needed only in the
cases below.

[source,objc]
----
@implementation Person
// Nothing needed: _name, -name and -setName: all exist.
@end
----

`@synthesize` becomes necessary when:

[source,objc]
----
@implementation Person

// 1. You implemented BOTH accessors yourself for a readwrite property --
//    autosynthesis then does not run, so ask for the ivar explicitly.
@synthesize name = _name;

// 2. You want a differently-named backing ivar.
@synthesize age = age_;

// 3. The property comes from a protocol (autosynthesis does not cover those).
@synthesize delegate = _delegate;

@end
----

`@dynamic` is the opposite declaration: *do not generate anything, the accessors will exist at run time*. It
silences the missing-implementation warning and is what `NSManagedObject` subclasses in Core Data use, along
with any class that supplies accessors through `+resolveInstanceMethod:`:

[source,objc]
----
@implementation Record
@dynamic identifier;      // Core Data (or your own runtime code) provides the accessors
@end
----

See
xref:programming-languages/objective-c/dynamic-method-resolution-and-forwarding.adoc[Dynamic Method Resolution
and Forwarding].

== Backing Ivars and When to Bypass the Accessor

The synthesised ivar is `_propertyName`. Use it directly in exactly two places, and use the property
everywhere else:

[source,objc]
----
- (instancetype)initWithName:(NSString *)name {
    self = [super init];
    if (self) {
        _name = [name copy];     // direct: a subclass override of -setName: must not run
    }                            // on a half-initialised object
    return self;
}

- (void)dealloc {
    [_connection invalidate];    // direct: same reasoning, in reverse
}

- (void)rename:(NSString *)newName {
    self.name = newName;         // everywhere else: go through the accessor, so that
}                                // KVO notifications, copy semantics and overrides all apply
----

Note the `[name copy]` in the initializer: assigning to the ivar directly bypasses the setter, so the `copy`
attribute's behaviour must be reproduced by hand.

=== Custom Accessors

[source,objc]
----
// A lazily-computed readonly property.
- (NSString *)fullDescription {
    if (!_fullDescription) {
        _fullDescription = [NSString stringWithFormat:@"%@ (%ld)", self.name, (long)self.age];
    }
    return _fullDescription;
}

// A setter with a side effect. Note it must honour the declared `copy` policy.
- (void)setName:(NSString *)name {
    if (_name != name) {
        _name = [name copy];
        [self invalidateCache];
    }
}
----

A custom accessor takes over the contract completely -- including the memory-management policy the attribute
promised. Getting that wrong (storing without `copy` on a `copy` property) is a silent bug.

== Dot Syntax versus Accessor Messages

These are *identical* -- dot syntax is pure sugar, resolved to the same message send:

[source,objc]
----
person.name = @"Ada";       //  [person setName:@"Ada"]
NSString *n = person.name;  //  [person name]
----

The conventions that keep dot syntax readable:

* Use it for *properties* -- state access that is cheap and side-effect-free.
* Use bracket syntax for *methods*, especially anything that performs work: `[array count]` on a plain method,
  `[connection start]`, `[view layoutIfNeeded]`.
* Chaining through dots (`a.b.c.d`) is legal but hides four message sends, each of which may return `nil`;
  break it up when it stops being obvious.

Dot syntax on a `struct`-typed property has a genuine trap:

[source,objc]
----
// view.frame.origin.x = 10;        // compile error: cannot assign to a returned struct
CGRect f = view.frame;              // read it out
f.origin.x = 10;                    // mutate the copy
view.frame = f;                     // write it back
----

== KVC-Compliant Accessor Naming

Key-value coding finds accessors *by name*. Following the conventional naming is therefore what makes a
property work with KVC, KVO, bindings, `NSPredicate`, `NSSortDescriptor` and Core Data -- and the compiler
cannot warn you when you break it:

[cols="1,2",options="header"]
|===
| Purpose | Required name for key `name`

| Getter | `-name` or `-isName` (Booleans)
| Setter | `-setName:`
| Direct ivar | `_name`, `_isName`, `name` or `isName`
| Validation | `-validateName:error:`
| To-many count | `-countOfItems`
| To-many access | `-objectInItemsAtIndex:` / `-itemsAtIndexes:`
| To-many mutation | `-insertObject:inItemsAtIndex:` / `-removeObjectFromItemsAtIndex:`
|===

Declaring a `@property` gives you the first three for free, which is why properties and KVC fit together so
naturally. See
xref:programming-languages/objective-c/key-value-coding-and-observing.adoc[Key-Value Coding and Observing].

== Private Properties and the `readonly`/`readwrite` Idiom

A **class extension** -- `@interface ClassName ()` with empty parentheses, placed in the `.m` -- declares
properties and methods that are invisible to clients:

[source,objc]
----
// Person.m
@interface Person ()
@property (nonatomic, strong) NSMutableArray *mutableHistory;   // entirely private
@property (nonatomic, assign) BOOL isLoading;
@end
----

The most useful application of this is redeclaring a public `readonly` property as `readwrite` privately, so
the value can be maintained internally while remaining immutable to everyone else:

[source,objc]
----
// Download.h -- clients can read progress but never set it.
@interface Download : NSObject
@property (nonatomic, readonly) double progress;
@end

// Download.m -- internally it is settable.
@interface Download ()
@property (nonatomic, readwrite) double progress;
@end

@implementation Download
- (void)didReceiveBytes:(NSUInteger)n {
    self.progress = (double)self.received / self.expected;   // setter exists here
}
@end
----

A closely related pattern keeps a collection immutable in public and mutable in private:

[source,objc]
----
// Public: an immutable snapshot.
@property (nonatomic, readonly, copy) NSArray<Item *> *items;

// Private: the real storage.
@interface Library ()
@property (nonatomic, strong) NSMutableArray<Item *> *mutableItems;
@end

@implementation Library
- (NSArray<Item *> *)items { return [self.mutableItems copy]; }
@end
----

Class extensions are covered alongside categories in
xref:programming-languages/objective-c/categories-and-extensions.adoc[Categories and Extensions].

== See Also

* xref:programming-languages/objective-c/classes-and-objects.adoc[Classes and Objects] -- ivars, visibility
  and initialisation.
* xref:programming-languages/objective-c/categories-and-extensions.adoc[Categories and Extensions] -- the
  class extension in full.
* xref:programming-languages/objective-c/automatic-reference-counting.adoc[Automatic Reference Counting] --
  what `strong`, `weak` and `copy` actually do.
* xref:programming-languages/objective-c/key-value-coding-and-observing.adoc[Key-Value Coding and Observing]
  -- why accessor naming matters.

== References

* https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ProgrammingWithObjectiveC/EncapsulatingData/EncapsulatingData.html[Apple
  -- Programming with Objective-C: Encapsulating Data].
* https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjectiveC/Chapters/ocProperties.html[Apple
  -- The Objective-C Programming Language: Declared Properties].
* https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/KeyValueCoding/AccessorConventions.html[Apple
  -- Key-Value Coding Programming Guide: Accessor Search Patterns].
* https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/CodingGuidelines/CodingGuidelines.html[Apple
  -- Coding Guidelines for Cocoa].
* https://clang.llvm.org/docs/AutomaticReferenceCounting.html[Clang -- Automatic Reference Counting].
