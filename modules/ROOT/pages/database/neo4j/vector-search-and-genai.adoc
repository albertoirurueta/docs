= Vector Search and GenAI
:description: Storing embeddings in Neo4j's native VECTOR type, running approximate nearest-neighbor queries with the Cypher SEARCH clause, and the GraphRAG pattern that layers graph traversal on top of vector retrieval for LLM applications.
:keywords: neo4j, vector search, vector index, VECTOR type, SEARCH clause, ANN, approximate nearest neighbor, embeddings, GraphRAG, retrieval-augmented generation, RAG, neo4j-graphrag-python, LangChain4j, Spring AI, GenAI ecosystem

include::partial$neo4j-disclaimer.adoc[]

Neo4j stores an embedding as a native vector property and indexes it for approximate nearest-neighbor (ANN)
search like any other document or vector store, but the property graph underneath is what turns plain vector
similarity into *GraphRAG* -- retrieval that also walks relationships for context a bag of nearest embeddings
cannot capture on its own.

== The VECTOR type and vector indexes

An embedding is stored as a `VECTOR`-typed property, built with the `vector()` function from a list of
numbers, a dimension count and a coordinate type:

[source,cypher]
----
MATCH (m:Movie {title: "The Matrix"})
SET m.embedding = vector($embedding, 1536, FLOAT);
----

A `VECTOR` property only becomes ANN-searchable once a *vector index* exists over it -- see
xref:database/neo4j/indexes-and-constraints.adoc[Indexes and constraints] for the full `CREATE VECTOR INDEX`
syntax, the `vector.dimensions` / `vector.similarity_function` options, and how a vector index compares to
Neo4j's other index types. This page assumes that index already exists and focuses on querying it, and on
what a graph adds once retrieval has to go beyond a flat top-k list. Full details on the type itself are in
https://neo4j.com/docs/cypher-manual/current/values-and-types/vector/[the Cypher manual's Vector type
reference].

== Querying with the Cypher SEARCH clause

The modern way to run an ANN query in Cypher is the `SEARCH` clause: it binds a variable from a `MATCH`
pattern to the nearest neighbors of a query vector inside a named vector index, optionally yielding a
similarity score:

[source,cypher]
----
MATCH (m:Movie)
  SEARCH m IN (
    VECTOR INDEX movie_plot_embedding
    FOR vector($queryEmbedding, 1536, FLOAT)
    LIMIT 5
  ) SCORE AS similarity
RETURN m.title AS title, similarity
ORDER BY similarity DESC;
----

Older code may still call the `db.index.vector.queryNodes(indexName, k, queryVector)` procedure for the same
lookup -- it still runs, but `SEARCH` is the current, superseding way to express a vector query as a normal
part of a Cypher pattern rather than a separate procedure call. See
https://neo4j.com/docs/cypher-manual/current/clauses/search/[the Cypher manual's SEARCH clause reference] for
the full grammar, including combining `SEARCH` with `WHERE` pre-filters and other clauses in the same query.

== GraphRAG: vector retrieval plus graph expansion

*GraphRAG* is the pattern of following a vector-similarity retrieval step with a graph traversal step before
handing context to an LLM: the vector search finds the chunks or entities most similar to the query embedding,
and the graph expansion pulls in the neighbors, related entities, or provenance those hits connect to --
context a nearest-neighbor list alone has no way to express. The combined result, not just the raw matches,
becomes the context passed to the LLM prompt.

[source,cypher]
----
// 1. vector retrieval: the chunks most similar to the query embedding
MATCH (chunk:Chunk)
  SEARCH chunk IN (
    VECTOR INDEX chunk_embedding
    FOR vector($queryEmbedding, 1536, FLOAT)
    LIMIT 5
  ) SCORE AS similarity
// 2. graph expansion: entities each chunk mentions, and their close neighbors
MATCH (chunk)-[:MENTIONS]->(entity)-[:RELATED_TO*1..2]-(context)
RETURN chunk.text AS chunk, similarity, collect(DISTINCT context.name) AS relatedContext
ORDER BY similarity DESC;
----

image::neo4j-graphrag-pipeline.svg[A left-to-right GraphRAG pipeline: a query is embedded and matched by vector retrieval, the graph is expanded from those matches, the combined result becomes LLM context, and the LLM returns an answer,width=760,role=text-center]

The official https://neo4j.com/docs/neo4j-graphrag-python/current/[`neo4j-graphrag-python`] package packages
this pattern -- embedding, vector retrieval, optional graph expansion, and prompt assembly -- as a Python
library purpose-built for Neo4j. On the JVM side, the
https://neo4j.com/labs/genai-ecosystem/langchain4j/[LangChain4j integration] wires Neo4j in as a vector store
and graph-augmented retriever for LangChain4j pipelines, and the
https://neo4j.com/labs/genai-ecosystem/spring-ai/[Spring AI integration] does the same for Spring AI's
`VectorStore` abstraction -- both sit above the same driver and object mapping described in
xref:backend/springboot/spring-data-neo4j.adoc[Spring Data Neo4j] rather than replacing it. The broader
catalog of drivers, frameworks and tools in this space is indexed at
https://neo4j.com/labs/genai-ecosystem/[the Neo4j GenAI Ecosystem], and
https://neo4j.com/developer/genai-ecosystem/vector-search/[the Vector Search developer guide] walks through
the retrieval half of the pattern end to end.

A dedicated treatment of building LLM-powered applications on Neo4j exists -- see the bibliography.
For vector/RAG retrieval that doesn't need graph expansion, xref:database/redis/vector-search.adoc[Redis:
Vector search] is the Redis alternative.
