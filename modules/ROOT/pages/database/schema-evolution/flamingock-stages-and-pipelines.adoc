= Flamingock: Stages & Pipelines
:description: Grouping Changes into sequential, ordered stages, and pipelines of multiple stages, for environment/lifecycle-scoped rollout.
:keywords: Flamingock stages, Flamingock pipeline, setup and stages

include::partial$schema-evolution-disclaimer.adoc[]

== Stages

A *stage* is an ordered group of Changes that execute sequentially, in the order their `order` attribute defines
-- the same ordering guarantee Mongock gives a flat list of `@ChangeUnit` classes
(xref:database/schema-evolution/mongock-changeunits.adoc[Mongock: ChangeUnits]), but scoped to a named subset
rather than everything Flamingock finds.

== Pipelines

A *pipeline* is one or more stages. Flamingock guarantees ordering *within* a stage, but explicitly does *not*
guarantee ordering *across* stages -- two stages can, depending on configuration, execute independently of each
other's internal ordering. This is the key structural difference from a single flat changelog: grouping into
stages lets you say "these changes must happen in this order" for one subset while leaving a different subset
free to run on its own schedule.

== Why This Matters for Lifecycle-Scoped Rollout

Splitting changes into stages maps naturally onto deployment lifecycle phases. A common shape is a "pre-deploy"
stage (schema changes the new application code depends on, which must complete before the new code starts) and a
"post-deploy" stage (cleanup, backfills, or changes that are safe to apply once the new code is already running)
-- the same expand/contract shape covered in
xref:database/schema-evolution/best-practices.adoc[Best Practices]:

[mermaid]
....
flowchart TB
    subgraph Stage1["Stage: pre-deploy (sequential, ordered)"]
        C1[Change A] --> C2[Change B] --> C3[Change C]
    end
    subgraph Stage2["Stage: post-deploy (sequential, ordered)"]
        C4[Change D] --> C5[Change E]
    end
    Stage1 -.order not guaranteed across stages.-> Stage2
....

Configuring stages is done via Flamingock's setup configuration -- see
https://docs.flamingock.io/flamingock-library-config/setup-and-stages[Setup & Stages] for the exact
configuration shape and how a pipeline ties multiple stages together at startup.
