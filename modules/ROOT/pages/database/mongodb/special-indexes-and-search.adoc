= Text, wildcard, geospatial & Atlas search
:description: MongoDB's special-purpose indexes -- text indexes with $text and $meta textScore, wildcard indexes for unpredictable field names, 2dsphere and legacy 2d geospatial indexes with $near / $geoWithin / $geoIntersects, and hashed indexes -- plus an overview of Atlas Search and Atlas Vector Search.
:keywords: mongodb, text index, $text, $search, $meta textScore, weights, wildcard index, attribute pattern, 2dsphere, GeoJSON, $near, $nearSphere, $geoWithin, $geoIntersects, 2d index, hashed index, hashed shard key, atlas search, atlas vector search, $vectorSearch, semantic search, embeddings

include::partial$mongodb-disclaimer.adoc[]

Beyond ordinary single-field and compound indexes, MongoDB has index types built for specific access patterns:
full-text matching, unpredictable field names, points and shapes on a sphere, and hash-distributed keys. This
page covers each with a runnable example, then points to the Atlas-only search products. For the general index
model and `explain()` see xref:database/mongodb/indexes.adoc[Indexing & query performance].

== Text indexes and $text

A *text index* tokenises string fields (and string elements of arrays) so `$text` can match by word rather
than by exact string. Declare it with the special `"text"` direction; use `"$**"` as the field to index every
string field. See https://www.mongodb.com/docs/manual/core/indexes/index-types/index-text/[Text indexes] and
https://www.mongodb.com/docs/manual/text-search/[Text search].

[source,javascript]
----
db.articles.createIndex({ title: "text", body: "text" })

db.articles.find({ $text: { $search: "mongodb index" } })   // matches "indexes", "indexing" via stemming
----

A collection may have *only one text index*, though that index can span many fields. Use `$meta: "textScore"`
to project and sort by relevance. See
https://www.mongodb.com/docs/manual/reference/operator/query/text/[`$text`] and
https://www.mongodb.com/docs/manual/reference/operator/aggregation/meta/[`$meta`].

[source,javascript]
----
db.articles.find(
  { $text: { $search: "mongodb index" } },
  { score: { $meta: "textScore" }, title: 1 }
).sort({ score: { $meta: "textScore" } })
----

The index's `default_language` sets the stemming and stop-word rules; a per-document `language` field can
override it. `weights` scale each field's contribution to the score, so a match in the title can outrank a
match in the body.

[source,javascript]
----
db.articles.createIndex(
  { title: "text", body: "text" },
  { default_language: "english", weights: { title: 10, body: 1 } }
)
----

[NOTE]
====
The built-in text index is adequate for simple keyword lookup. For real search workloads -- relevance tuning,
analyzers, autocomplete, faceting, fuzzy matching -- <<Atlas Search and Atlas Vector Search>> supersedes it
and is the recommended path on Atlas.
====

== Wildcard indexes

A *wildcard index* uses the `"$**"` key to index fields whose names are not known in advance -- an
attribute bag such as `attributes.color`, `attributes.ramGb`, `attributes.warrantyMonths` that differs per
document. `wildcardProjection` restricts it to (or excludes) a sub-tree. See
https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/[Wildcard indexes].

[source,javascript]
----
db.products.createIndex({ "attributes.$**": 1 })

db.products.find({ "attributes.color": "blue" })          // uses the wildcard index
db.products.find({ "attributes.ramGb": { $gte: 16 } })    // and so does this
----

Trade-offs: a wildcard index can be large (one entry per indexed leaf field per document) and slows writes
accordingly; it supports only one queried field at a time (it is not a substitute for a compound index); it
cannot back a sort on an unspecified field; and it never covers a query. Reach for it only when the field
names really are open-ended -- otherwise name the fields in an ordinary index.

== Geospatial indexes

A *2dsphere* index indexes points and shapes stored as https://geojson.org/[GeoJSON] objects on an
Earth-like sphere, so distances and containment are computed correctly across the globe. It supports
`$near` / `$nearSphere` (nearest-first, optionally bounded by `$maxDistance` in metres), `$geoWithin`
(inside a polygon), and `$geoIntersects` (shares any point with a geometry). See
https://www.mongodb.com/docs/manual/geospatial-queries/[Geospatial queries] and
https://www.mongodb.com/docs/manual/core/indexes/index-types/index-geospatial/index-2dsphere/[2dsphere
indexes].

[source,javascript]
----
db.places.insertOne({
  name: "Cafe",
  location: { type: "Point", coordinates: [ -3.7038, 40.4168 ] }   // [ longitude, latitude ]
})
db.places.createIndex({ location: "2dsphere" })

// Nearest places within 1 km
db.places.find({
  location: {
    $nearSphere: {
      $geometry: { type: "Point", coordinates: [ -3.7038, 40.4168 ] },
      $maxDistance: 1000
    }
  }
})

// Places inside a neighbourhood polygon
db.places.find({
  location: {
    $geoWithin: {
      $geometry: { type: "Polygon", coordinates: [ [ [ -3.71, 40.41 ], [ -3.69, 40.41 ], [ -3.69, 40.43 ], [ -3.71, 40.43 ], [ -3.71, 40.41 ] ] ] }
    }
  }
})
----

See https://www.mongodb.com/docs/manual/reference/operator/query/near/[`$near`],
https://www.mongodb.com/docs/manual/reference/operator/query/geoWithin/[`$geoWithin`], and
https://www.mongodb.com/docs/manual/reference/operator/query/geoIntersects/[`$geoIntersects`].

The legacy *2d* index indexes coordinate pairs on a flat plane (`[ x, y ]` legacy pairs, not GeoJSON). Use it
only for planar data such as a game map or floor plan, not for points on the Earth. See
https://www.mongodb.com/docs/manual/core/indexes/index-types/index-geospatial/index-2d/[2d indexes].

[source,javascript]
----
db.board.createIndex({ pos: "2d" })
db.board.find({ pos: { $geoWithin: { $box: [ [ 0, 0 ], [ 100, 100 ] ] } } })
----

== Hashed indexes

A *hashed* index stores the hash of a single field's value rather than the value itself. Its main use is a
*hashed shard key*, which spreads monotonically increasing keys (such as `ObjectId` or a timestamp) evenly
across shards instead of piling every new document onto one chunk. Hashed indexes support equality matches but
not range queries or sorts on the field. See
https://www.mongodb.com/docs/manual/core/indexes/index-types/index-hashed/[Hashed indexes], and for how a
shard key is chosen see xref:database/mongodb/sharding.adoc[Sharding].

[source,javascript]
----
db.events.createIndex({ _id: "hashed" })
sh.shardCollection("app.events", { _id: "hashed" })
----

== Atlas Search and Atlas Vector Search

These are *Atlas-only* services -- available on MongoDB Atlas, not in the self-managed server -- so they are
described here only in outline.

*Atlas Search* embeds an Apache Lucene index alongside the collection and exposes it through the `$search`
aggregation stage. It provides analyzers, relevance scoring, fuzzy matching, autocomplete, highlighting, and
facets -- the capabilities the built-in text index lacks -- and stays in sync with the collection
automatically. Use it for user-facing full-text search. See
https://www.mongodb.com/docs/atlas/atlas-search/[Atlas Search].

*Atlas Vector Search* indexes dense vector embeddings and exposes approximate nearest-neighbour queries
through the `$vectorSearch` stage, for semantic search, recommendations, and retrieval-augmented generation.
You generate embeddings with an external model, store them as an array field, and query with a query vector
through `$vectorSearch`. See
https://www.mongodb.com/docs/atlas/atlas-vector-search/[Atlas Vector Search] and the
https://www.mongodb.com/docs/atlas/atlas-vector-search/vector-search-stage/[`$vectorSearch` stage reference]
for the full syntax.
