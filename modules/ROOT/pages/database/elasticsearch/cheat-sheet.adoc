= Elasticsearch Cheat Sheet
:description: A single-page, colour-coded summary of the most commonly used Elasticsearch essentials covered by this reference -- the curl / Dev Tools request shape with ?pretty and ?filter_path, create-index with settings and mappings, text vs. keyword and the multi-field idiom, the analysis chain, the CRUD and _bulk NDJSON skeletons with if_seq_no / if_primary_term, _search in query vs. filter context, the match / multi_match / term / range / bool query skeletons, function_score / sort / search_after with a point-in-time, the metrics / terms / date_histogram aggregation skeleton, nested vs. join, top-level knn and retrievers, an ES|QL one-liner, the _cat one-liners, an alias swap and the ILM phase strip, and a snapshot / restore skeleton -- with a downloadable printable PDF version.
:keywords: elasticsearch cheat sheet, quick reference, printable elasticsearch reference, kibana dev tools, curl, pretty, filter_path, create index, settings, mappings, text vs keyword, keyword, multi-field, fields, analyzer, char filter, tokenizer, token filter, analysis chain, index api, get, update, delete, bulk, ndjson, if_seq_no, if_primary_term, optimistic concurrency control, search api, query context, filter context, match, multi_match, term, terms, range, bool, must, should, filter, must_not, minimum_should_match, function_score, sort, search_after, point in time, pit, aggregations, terms aggregation, date_histogram, sub-aggregation, nested, join, parent-child, has_child, has_parent, knn, dense_vector, retriever, rrf, esql, es|ql, cat api, _cat/health, _cat/indices, _cat/shards, alias, aliases, ilm, index lifecycle management, hot warm cold frozen, snapshot, restore, slm

include::partial$elasticsearch-disclaimer.adoc[]

A single-page, colour-coded summary of the Elasticsearch essentials -- the `curl` / Kibana Dev Tools request
shape with `?pretty` and `?filter_path`, `PUT` create-index with `settings` and `mappings`, `text` vs. `keyword`
and the multi-field idiom, the character filters / tokenizer / token filters analysis chain, the document CRUD
and `_bulk` NDJSON skeletons with `if_seq_no` / `if_primary_term` optimistic concurrency control, `_search` in
query vs. filter context, the `match` / `multi_match` / `term` / `range` / `bool` query skeletons,
`function_score` / `sort` / `search_after` with a point-in-time, the metrics / `terms` / `date_histogram`
aggregation skeleton, `nested` vs. `join`, top-level `knn` and a retriever, an ES|QL one-liner, the
`_cat/health?v` / `_cat/indices?v` / `_cat/shards?v` one-liners, an alias swap and the ILM phase strip, a
snapshot / restore skeleton, and a "query vs. filter context" decision strip -- handy for quick memorization or
as a printable desk reference.

For the full explanation, every option, and additional examples, see the dedicated pages:

*Getting started* --
xref:database/elasticsearch/getting-started.adoc[What Elasticsearch is & how to run it].

*Data model, mapping & analysis* --
xref:database/elasticsearch/documents-and-indices.adoc[Documents, indices & the inverted index],
xref:database/elasticsearch/mapping-and-field-types.adoc[Mapping & field types], and
xref:database/elasticsearch/text-analysis.adoc[Text analysis: analyzers, tokenizers & token filters].

*Indexing* --
xref:database/elasticsearch/indexing-crud-and-bulk.adoc[Indexing, CRUD, bulk & concurrency control] and
xref:database/elasticsearch/ingest-pipelines.adoc[Ingest pipelines & processors].

*Searching & analytics* --
xref:database/elasticsearch/search-api-and-pagination.adoc[The search API, paging & sorting],
xref:database/elasticsearch/query-dsl-full-text.adoc[Full-text queries],
xref:database/elasticsearch/query-dsl-term-level.adoc[Term-level queries],
xref:database/elasticsearch/compound-queries-and-relevance.adoc[Compound queries, relevance & scoring],
xref:database/elasticsearch/aggregations.adoc[Aggregations],
xref:database/elasticsearch/joins-and-relationships.adoc[Joins & relationships],
xref:database/elasticsearch/search-extras.adoc[Search extras: highlighting, suggesters, collapse & percolation],
xref:database/elasticsearch/geospatial.adoc[Geospatial data & queries],
xref:database/elasticsearch/vector-and-semantic-search.adoc[Vector & semantic search], and
xref:database/elasticsearch/query-languages-and-scripting.adoc[Query languages & scripting].

*Distributed model & operations* --
xref:database/elasticsearch/cluster-nodes-and-shards.adoc[Cluster, nodes & shards],
xref:database/elasticsearch/index-lifecycle-and-scaling.adoc[Index lifecycle & scaling],
xref:database/elasticsearch/performance-tuning.adoc[Performance & the storage/caching model],
xref:database/elasticsearch/administration-monitoring-and-snapshots.adoc[Administration, monitoring & snapshots],
xref:database/elasticsearch/security.adoc[Securing a cluster], and
xref:database/elasticsearch/clients-and-rest-conventions.adoc[Clients & REST API conventions].

xref:attachment$elasticsearch-cheat-sheet.pdf[Download the Elasticsearch Cheat Sheet (PDF)]
