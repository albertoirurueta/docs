= Spring Boot Messaging: Overview
:description: Routes through Spring's messaging surface -- spring-messaging's shared Message/@Payload/@Header/template model, the Kafka/AMQP/JMS/Pulsar starters, and where Spring for Apache Kafka, Spring AMQP, Spring Cloud Stream and Spring Integration each fit -- linking out to the deeper pages that already cover each one.
:keywords: Spring messaging overview, spring-messaging, Message, MessageHeaders, @Payload, @Header, @KafkaListener, @RabbitListener, @JmsListener, spring-kafka, spring-boot-starter-amqp, spring-boot-starter-artemis, spring-boot-starter-activemq, spring-boot-starter-pulsar, spring-boot-starter-integration, Spring Cloud Stream, Spring Integration, KafkaTemplate, RabbitTemplate, JmsTemplate, PulsarTemplate, auto-configuration, spring.kafka, spring.rabbitmq, Testcontainers, ServiceConnection, Micrometer observation, Reactor Kafka

include::partial$messaging-disclaimer.adoc[]

Every other page under xref:backend/springboot/messaging-kafka.adoc[Messaging with Kafka] and
xref:backend/springboot/api-first-messaging.adoc[API-First: Messaging] dives straight into one broker's Spring
integration. This page steps back one level: it is the map of Spring's whole messaging surface -- the shared
`spring-messaging` abstraction every `@XxxListener` annotation builds on, which starter pulls in which broker,
how Spring for Apache Kafka, Spring AMQP, Spring Cloud Stream, and Spring Integration relate to and differ from
one another, and where the deeper, broker-specific pages already living in this documentation set fit into that
picture. The baseline across this whole section is Spring Boot 4.1.x on Java 25 (Java 21 remains the current
LTS most production deployments still target), pulling in Spring for Apache Kafka 4.1.x, Spring AMQP 4.1.x, and
Spring Cloud Stream 5.0.x, against Apache Kafka 4.3.x and RabbitMQ 4.3.x brokers.

== The shared `spring-messaging` model

Underneath every broker-specific annotation Spring Boot auto-configures, there is one shared abstraction module,
`spring-messaging`, that the Kafka, AMQP, and JMS integrations all build on rather than reinventing their own
message shape. A `Message<T>` pairs a typed `payload` with `MessageHeaders`, a read-only, string-keyed map of
metadata that travels alongside the payload -- a Kafka record's key, partition, and offset; an AMQP delivery's
routing key and content type; a JMS message's correlation ID -- without the application code needing to know
which broker produced them. Listener methods rarely handle a raw `Message<T>` directly, though: an argument
annotated `@Payload` binds to the (optionally converted) message body, and one annotated `@Header` binds to a
single header by name, so a method signature reads as a plain, typed parameter list instead of unwrapping an
envelope by hand.

`@KafkaListener`, `@RabbitListener`, and `@JmsListener` are three broker-specific instances of exactly this one
idea: annotate a method, Spring Boot auto-configures a container that polls or subscribes on the application's
behalf, converts the incoming payload, and invokes the method with `@Payload`/`@Header`-annotated arguments
resolved from the message and its headers. The *template* pattern mirrors this symmetrically on the sending
side -- `KafkaTemplate`, `RabbitTemplate`/`AmqpTemplate`, `JmsTemplate` (or the newer `JmsClient`), and
`PulsarTemplate` all expose the same shape of `send`/`convertAndSend` operation, auto-configured from
properties with no manual `@Bean` required for the common case. The three listener families side by side:

[source,java]
----
@KafkaListener(topics = "orders.placed", groupId = "orders-service")
public void onOrderPlaced(
        @Payload OrderPlacedEvent event,
        @Header(KafkaHeaders.RECEIVED_KEY) String key) {
    // event = the converted record value, key = the record's Kafka key
}
----

See https://docs.spring.io/spring-boot/reference/messaging/kafka.html[Spring Boot Reference -- Apache Kafka
Support] for `@KafkaListener` and the auto-configured `KafkaListenerContainerFactory`.

[source,java]
----
@RabbitListener(queues = "orders.placed")
public void onOrderPlaced(
        @Payload OrderPlacedEvent event,
        @Header(AmqpHeaders.RECEIVED_ROUTING_KEY) String routingKey) {
    // event = the converted message body, routingKey = the AMQP routing key it arrived on
}
----

See https://docs.spring.io/spring-boot/reference/messaging/amqp.html[Spring Boot Reference -- AMQP] for
`@RabbitListener` and the auto-configured `RabbitListenerContainerFactory`.

[source,java]
----
@JmsListener(destination = "orders.placed")
public void onOrderPlaced(
        @Payload OrderPlacedEvent event,
        @Header(JmsHeaders.CORRELATION_ID) String correlationId) {
    // event = the converted message body, correlationId = the JMS correlation ID header
}
----

See https://docs.spring.io/spring-boot/reference/messaging/jms.html[Spring Boot Reference -- JMS] for
`@JmsListener` and the `JmsTemplate`/`JmsClient` auto-configuration. Full producer/consumer treatments of the
Kafka case, including manual and batch acknowledgment and dead-letter routing, already live on
xref:backend/springboot/messaging-kafka.adoc[Messaging with Kafka] -- this page does not repeat that material.

The figure below shows how these layers stack: application code depends either directly on a broker-specific
Spring project, or, when broker portability matters more than access to broker-specific features, on Spring
Cloud Stream's functional model sitting above it.

image::messaging-spring-layers.svg[Layered architecture diagram: application code calling @KafkaListener; @RabbitListener; or @JmsListener sits above an optional Spring Cloud Stream functional layer; which sits above Spring for Apache Kafka or Spring AMQP; which sit above the native kafka-clients or amqp-client libraries; which talk to the Apache Kafka or RabbitMQ broker,width=700,role=text-center]

== Starters: which dependency pulls in which broker

Spring Boot's messaging starters are not perfectly uniform -- Kafka in particular has no dedicated
`spring-boot-starter-kafka` artifact, a common assumption that does not hold. The Spring Boot reference states
this plainly: "Apache Kafka is supported by providing auto-configuration of the `spring-kafka` project" -- so
the correct dependency to add for Kafka is the plain `spring-kafka` artifact itself (as already shown on
xref:backend/springboot/messaging-kafka.adoc[Messaging with Kafka]), not a `spring-boot-starter-*` coordinate.
AMQP, Pulsar, and the two JMS brokers Spring Boot supports out of the box do each have a real starter:

[cols="1,3,3",options="header"]
|===
| Broker | Dependency to add | What it pulls in

| Apache Kafka
| `org.springframework.kafka:spring-kafka` (no dedicated Boot starter -- Boot auto-configures this project
  directly)
| `KafkaTemplate`, `@KafkaListener` container factories, `spring.kafka.*` auto-configuration.

| RabbitMQ / AMQP
| `org.springframework.boot:spring-boot-starter-amqp`
| `RabbitTemplate`/`AmqpTemplate`, `AmqpAdmin`, `CachingConnectionFactory`, `@RabbitListener` container
  factories.

| ActiveMQ Artemis (JMS)
| `org.springframework.boot:spring-boot-starter-artemis`
| The dependencies to connect to an existing ActiveMQ Artemis broker, plus the Spring JMS infrastructure.

| ActiveMQ "Classic" (JMS)
| `org.springframework.boot:spring-boot-starter-activemq`
| The dependencies to connect to an ActiveMQ "Classic" broker, plus the Spring JMS infrastructure.

| Apache Pulsar
| `org.springframework.boot:spring-boot-starter-pulsar`
| A `PulsarClient` bean, `PulsarTemplate`, `PulsarAdministration`, and `@PulsarListener` container support.
|===

See https://docs.spring.io/spring-boot/reference/messaging/kafka.html[Spring Boot Reference -- Apache Kafka
Support], https://docs.spring.io/spring-boot/reference/messaging/amqp.html[Spring Boot Reference -- AMQP],
https://docs.spring.io/spring-boot/reference/messaging/jms.html[Spring Boot Reference -- JMS], and
https://docs.spring.io/spring-boot/reference/messaging/pulsar.html[Spring Boot Reference -- Apache Pulsar
Support] for each starter's exact dependency coordinates and what it auto-configures.

== Choosing among Spring's messaging projects

Four different Spring projects can all plausibly sit between application code and a broker, and picking the
wrong one for a given job is the most common source of confusion in this area. They differ along three axes:
how much the abstraction hides about the underlying broker, how easily code written against it moves to a
*different* broker, and what programming model it exposes.

[cols="2,2,2,3",options="header"]
|===
| Project | Abstraction level | Broker portability | Programming model

| Spring for Apache Kafka
| Low -- close to the native `kafka-clients` API, with Kafka-specific concepts (partitions, offsets, consumer
  groups) fully exposed.
| None -- Kafka only, by design.
| `KafkaTemplate` for sending; `@KafkaListener`-annotated methods, driven by a listener container, for
  receiving.

| Spring AMQP
| Low -- close to the native AMQP 0-9-1 model (exchanges, queues, routing keys), with RabbitMQ extensions
  (quorum queues, streams) exposed where relevant.
| Limited -- targets AMQP-speaking brokers, RabbitMQ specifically for anything beyond the AMQP 0-9-1 baseline.
| `RabbitTemplate`/`AmqpTemplate` for sending; `@RabbitListener`-annotated methods for receiving.

| Spring Cloud Stream
| High -- a `java.util.function.Supplier`/`Function`/`Consumer` bean is bound to an input/output destination by
  configuration; the broker is swapped by swapping a *binder* dependency, not application code.
| Good -- the same functional bean runs against Kafka, RabbitMQ, Pulsar, or other binders, though broker-
  specific behavior (partitioning, dead-lettering) still surfaces through binder-specific properties.
| Functional -- beans, not annotated methods; `StreamBridge` for imperative sends from outside a bound
  function.

| Spring Integration
| Highest -- messages flow through a broker-agnostic pipeline of channels, endpoints, and adapters (files,
  HTTP, JMS, AMQP, and more, not only "a message broker" in the Kafka/RabbitMQ sense).
| Good, but broader in scope than pure broker portability -- the same flow abstraction unifies transports that
  are not brokers at all (a filesystem poller, an FTP endpoint).
| `IntegrationFlow` DSL or XML-configured channels/endpoints, with `spring-boot-starter-integration` providing
  Boot auto-configuration for the core (`@EnableIntegration`, the `TaskScheduler` polling beans rely on, and
  optional JMX/JDBC/RSocket support when those modules are present).
|===

As a rule of thumb: reach for Spring for Apache Kafka or Spring AMQP directly when the application is committed
to one broker and wants full access to that broker's own concepts; reach for Spring Cloud Stream when the same
business logic genuinely needs to run against more than one broker, or when the functional
`Supplier`/`Function`/`Consumer` style fits the team's conventions better than annotated listener methods; and
reach for Spring Integration when messaging is only one of several transports a flow needs to cross (a file
drop that ends up publishing to Kafka, an HTTP callback that triggers a JMS send). See
https://docs.spring.io/spring-boot/reference/messaging/spring-integration.html[Spring Boot Reference -- Spring
Integration] for the auto-configuration details in that last row.

== Auto-configuration, properties, and connecting to a broker

Spring Boot auto-configures the producer/consumer factories, templates, and listener container factories for
each broker entirely from properties -- `spring.kafka.*` for Kafka (bootstrap servers, consumer group,
producer/consumer/admin/streams/listener sub-namespaces) and `spring.rabbitmq.*` for AMQP (host, port,
virtual host, SSL, cache, listener sub-namespaces), with no manual `@Bean` definitions needed for the common
case; the Kafka side is already covered in depth, property by property, on
xref:backend/springboot/messaging-kafka.adoc[Messaging with Kafka]. The full property reference for both
namespaces lives in the generated appendix, not hand-copied here since it changes with every release: see
https://docs.spring.io/spring-boot/appendix/application-properties/index.html[Spring Boot Reference -- Common
Application Properties] and search either namespace.

Pointing that auto-configuration at a real broker for integration tests, rather than hand-managing connection
properties, is what Testcontainers' `@ServiceConnection` annotation is for: it detects a `@Container`-managed
`KafkaContainer` or `RabbitMQContainer` and registers the matching `ConnectionDetails` bean, which Spring Boot's
Kafka/AMQP auto-configuration then uses in place of `spring.kafka.*`/`spring.rabbitmq.*` properties -- with an
equivalent Docker Compose-based flow available for local development against the same images. That whole
workflow -- container setup, `@ServiceConnection`, and the Docker Compose alternative -- is covered end to end
on the dedicated xref:backend/messaging/spring-boot-testing-messaging.adoc[] page rather than repeated here.

== Observability with Micrometer

Both Spring for Apache Kafka and Spring AMQP integrate with Micrometer's *observation* API so that a producer
send or a listener invocation shows up as a span/metric pair automatically once a `Tracer`/`MeterRegistry` is
on the classpath, without hand-instrumenting each `@KafkaListener`/`@RabbitListener` method. Enabling it is a
one-line container-factory setting on each side:

[source,java]
----
@Bean
public ConcurrentKafkaListenerContainerFactory<String, OrderPlacedEvent> kafkaListenerContainerFactory(
        ConsumerFactory<String, OrderPlacedEvent> consumerFactory) {
    var factory = new ConcurrentKafkaListenerContainerFactory<String, OrderPlacedEvent>();
    factory.setConsumerFactory(consumerFactory);
    factory.getContainerProperties().setObservationEnabled(true);
    return factory;
}
----

See https://docs.spring.io/spring-kafka/reference/kafka/micrometer.html[Spring for Apache Kafka Reference --
Micrometer Observation] for the producer-side `KafkaTemplate` equivalent and the specific spans/tags recorded.

[source,java]
----
@Bean
public SimpleRabbitListenerContainerFactory rabbitListenerContainerFactory(
        ConnectionFactory connectionFactory) {
    var factory = new SimpleRabbitListenerContainerFactory();
    factory.setConnectionFactory(connectionFactory);
    factory.setObservationEnabled(true);
    return factory;
}
----

See https://docs.spring.io/spring-amqp/reference/amqp/receiving-messages/micrometer-observation.html[Spring
AMQP Reference -- Micrometer Observation] for the `RabbitTemplate` producer-side setting and the observation
naming conventions it follows.

== Reactive messaging

Reactive `Mono`/`Flux` pipelines have a Kafka path too: *Reactor Kafka* wraps `kafka-clients` in a
`KafkaReceiver`/`KafkaSender` pair that produces and consumes as backpressure-aware Reactor publishers instead
of the imperative `poll()` loop `KafkaConsumer` exposes directly, and Spring Cloud Stream's reactive Kafka
binder lets a bound function be written as `Function<Flux<Order>, Flux<OrderPlaced>>` rather than a plain
`Function<Order, OrderPlaced>`, for the same backpressure benefit inside the functional binding model described
above. Both sit on top of the same Reactor foundation covered in depth on
xref:backend/springboot/reactive-programming.adoc[]; this page does not repeat `Mono`/`Flux`, schedulers, or
backpressure semantics, only how they connect to messaging specifically.

== Already documented elsewhere on this site

This page is intentionally thin on runtime detail because that detail already has a home. The table below
points at where each closely related topic actually lives:

[cols="2,3",options="header"]
|===
| Topic | Page

| `KafkaTemplate`, transactional producers, `@KafkaListener` manual/batch acknowledgment, `DefaultErrorHandler`,
  retry, and dead-letter topics
| xref:backend/springboot/messaging-kafka.adoc[]

| Apache Avro schemas, the `avro-maven-plugin`, Confluent Schema Registry compatibility modes, and describing
  topics with AsyncAPI
| xref:backend/springboot/api-first-messaging.adoc[]

| SmallRye Reactive Messaging -- the Quarkus equivalent of this whole page's `@Incoming`/`@Outgoing`/`@Channel`
  model, connector-agnostic across Kafka, RabbitMQ, AMQP 1.0, Pulsar, and JMS
| xref:backend/quarkus/messaging.adoc[]

| AMQP-based remote chunking and remote partitioning for Spring Batch, wired over `RabbitTemplate` and
  `Amqp.inboundAdapter`
| xref:backend/spring-batch/spring-batch-integration.adoc[]
|===

== References

* https://docs.spring.io/spring-boot/reference/messaging/index.html[Spring Boot Reference -- Messaging]
* https://docs.spring.io/spring-boot/reference/messaging/kafka.html[Spring Boot Reference -- Apache Kafka
  Support]
* https://docs.spring.io/spring-boot/reference/messaging/amqp.html[Spring Boot Reference -- AMQP]
* https://docs.spring.io/spring-boot/reference/messaging/jms.html[Spring Boot Reference -- JMS]
* https://docs.spring.io/spring-boot/reference/messaging/pulsar.html[Spring Boot Reference -- Apache Pulsar
  Support]
* https://docs.spring.io/spring-boot/reference/messaging/spring-integration.html[Spring Boot Reference -- Spring
  Integration]
* https://docs.spring.io/spring-boot/appendix/application-properties/index.html[Spring Boot Reference -- Common
  Application Properties]
* https://docs.spring.io/spring-boot/reference/testing/testcontainers.html[Spring Boot Reference --
  Testcontainers]
* https://docs.spring.io/spring-kafka/reference/kafka/micrometer.html[Spring for Apache Kafka Reference --
  Micrometer Observation]
* https://docs.spring.io/spring-amqp/reference/amqp/receiving-messages/micrometer-observation.html[Spring AMQP
  Reference -- Micrometer Observation]
* https://docs.spring.io/spring-cloud-stream/reference/[Spring Cloud Stream Reference Documentation]
