= Images, Layers and Tags
:description: What a container image is -- read-only layers plus a configuration addressed by content digest -- layer sharing and copy-on-write, image references, tags versus digests and why latest is a trap, manifests and OCI image indexes for multi-platform images, and the image commands (pull, image ls, inspect, history, tag, rmi, save/load, export/import, commit, prune), labels and annotations.
:keywords: Docker image, image layers, copy-on-write, writable container layer, image digest, sha256, image tag, latest tag, image reference, registry namespace repository, image manifest, manifest list, OCI image index, multi-platform image, docker pull, docker image ls, docker image inspect, docker image history, docker tag, docker rmi, docker save, docker load, docker commit, docker image prune, labels, annotations

include::partial$docker-disclaimer.adoc[]

Everything Docker does starts from an image, and most surprises -- an image that "changed" without a rebuild, a
cache that never hits, a `docker pull` that downloads a different architecture than expected -- come from
misunderstanding what an image and its name actually are. This page explains the image model and the commands that
manage images, as implemented by Docker Engine 29.8.x and the OCI image specification.

== Images and containers

The two 2015 books in the xref:backend/docker/index.adoc#_bibliography[bibliography] draw the distinction the same
way, and it still holds: an *image* is a read-only template; a *container* is a running (or stopped) instance of it
with its own writable layer, process and configuration. One image can back any number of containers.

Concretely, an image is:

* an ordered list of *layers* -- each a tar archive of filesystem changes (files added, changed or deleted),
  produced by one filesystem-changing build step such as `RUN`, `COPY` or `ADD`;
* an image *configuration* -- JSON holding the default command (`Entrypoint`, `Cmd`), environment, working
  directory, user, exposed ports, labels, the target platform and the build history;
* a *manifest* tying the two together by digest.

Every piece is addressed by the SHA-256 digest of its content. Two images that share a base share those layers on
disk and in the registry, and a layer that already exists locally is never downloaded again.

image::docker-image-layers.svg[Two containers reading through the same stack of read-only image layers while each writes to its own writable layer,width=700]

When a container starts, the storage driver or snapshotter stacks the image's layers into one read-only view and
adds a thin writable layer on top. Reading a file finds it in the topmost layer that contains it. Writing to a file
that lives in a lower layer first *copies it up* into the writable layer (copy-on-write); deleting it records a
"whiteout". The writable layer disappears with the container, which is why persistent data belongs in volumes (see
xref:backend/docker/storage-volumes-and-bind-mounts.adoc[Storage: Volumes, Bind Mounts and tmpfs]). Derived from
https://docs.docker.com/get-started/docker-concepts/building-images/understanding-image-layers/[Understanding the
image layers] and https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-an-image/[What is an
image?].

[source,shell]
----
docker run -d --name c1 nginx:1.29
docker exec c1 sh -c 'echo changed > /usr/share/nginx/html/index.html'
docker container diff c1        # C = changed, A = added, D = deleted -- only the writable layer
docker rm -f c1                  # the change is gone with the container
----

Derived from https://docs.docker.com/reference/cli/docker/container/diff/[`docker container diff`].

== Image references: registry, namespace, repository, tag, digest

A full image reference is:

----
[registry[:port]/][namespace/]repository[:tag][@sha256:digest]
----

[cols="1,2,2",options="header"]
|===
| Part | Example | Default when omitted

| Registry
| `ghcr.io`, `registry.example.com:5000`
| `docker.io` (Docker Hub)

| Namespace
| `library`, `myorg`, `eclipse-temurin` (none)
| `library` on Docker Hub, which holds the Docker Official Images

| Repository
| `nginx`, `myorg/orders-service`
| -- (required)

| Tag
| `1.29`, `25-jre`, `1.4.0`
| `latest`

| Digest
| `@sha256:3f5e...`
| none
|===

So `nginx` means `docker.io/library/nginx:latest`, and `ghcr.io/myorg/orders-service:1.4.0` is fully explicit.

=== Tags versus digests, and why `latest` is a trap

A *tag* is a mutable pointer: the publisher can move `nginx:1.29` to a new build tomorrow (to ship a security fix,
for example), and `latest` is nothing more than the tag used when none is given -- it does not mean "newest" and
is not updated automatically. A *digest* is immutable: `nginx@sha256:...` always resolves to exactly the same bytes,
and pulling by digest verifies them.

* Use specific version tags (`eclipse-temurin:25-jre`, `postgres:18.1`) in `FROM` lines and `compose.yaml` so builds
  are understandable, and pin by digest (`FROM eclipse-temurin:25-jre@sha256:...`) where reproducibility matters
  more than automatically receiving patches.
* Never deploy `latest`: two nodes pulling at different times can run different code under the same name, and a
  rollback to "the previous `latest`" is impossible.
* Docker Hub and other registries offer *immutable tags* that cannot be overwritten once pushed (see
  xref:backend/docker/registries-and-docker-hub.adoc[Registries and Docker Hub]).

[source,shell]
----
docker pull nginx:1.29
docker image inspect --format '{{index .RepoDigests 0}}' nginx:1.29   # nginx@sha256:...
docker pull nginx@sha256:<digest-from-above>                           # always the same content
----

Derived from https://docs.docker.com/reference/cli/docker/image/pull/#pull-an-image-by-digest-immutable-identifier[Pull
an image by digest].

=== Manifests, manifest lists and multi-platform images

A single name such as `eclipse-temurin:25-jre` works on an x86-64 laptop, an Apple-silicon Mac and an ARM server
because it points to an *image index* (called a *manifest list* in Docker's original format): a small document
listing one manifest per platform. The client picks the entry that matches its platform, then fetches that
manifest's configuration and layers.

[mermaid]
....
flowchart LR
  ref["eclipse-temurin:25-jre"] --> idx["Image index / manifest list<br/>sha256:aa11..."]
  idx -->|linux/amd64| m1["Manifest amd64<br/>sha256:bb22..."]
  idx -->|linux/arm64| m2["Manifest arm64<br/>sha256:cc33..."]
  idx -.->|attestation manifest| att["SBOM and provenance"]
  m2 --> cfg["Config JSON<br/>entrypoint, env, user, history"]
  m2 --> l1["Layer 1 blob"]
  m2 --> l2["Layer 2 blob"]
  m2 --> l3["Layer 3 blob"]
....

[source,shell]
----
docker buildx imagetools inspect eclipse-temurin:25-jre       # the index and every platform in it
docker pull --platform linux/amd64 eclipse-temurin:25-jre      # a specific platform (runs emulated on arm64)
docker image inspect --format '{{.Os}}/{{.Architecture}}' eclipse-temurin:25-jre
----

Derived from https://docs.docker.com/build/building/multi-platform/[Multi-platform builds] and
https://docs.docker.com/reference/cli/docker/buildx/imagetools/inspect/[`docker buildx imagetools inspect`].
Building such images is covered in xref:backend/docker/buildkit-and-buildx.adoc[BuildKit and Buildx]. With the
containerd image store (the default on fresh Engine 29 installs) the local store keeps the whole index, including
attestations; the classic store keeps only the pulled platform.

== Managing images

[source,shell]
----
docker pull postgres:18.1                  # download (layers already present are skipped)
docker image ls                            # Engine 29: a collapsed tree view, one row per image with its platforms
docker image ls --tree                     # expand every platform variant and its size
docker image ls --all                      # include untagged (dangling) images, hidden by default since 29
docker image ls --filter reference='postgres:*' --format '{{.Repository}}:{{.Tag}} {{.Size}}'
docker image inspect postgres:18.1         # full configuration JSON
docker image history postgres:18.1         # one row per build step and the layer it produced
docker tag postgres:18.1 registry.example.com/db/postgres:18.1   # a second name for the same image ID
docker image rm registry.example.com/db/postgres:18.1           # removes the name; data goes with the last name
docker image prune                         # remove dangling images
docker image prune --all --filter until=168h   # remove all unused images created more than a week ago
----

Derived from https://docs.docker.com/reference/cli/docker/image/[`docker image`] (and its `ls`, `inspect`,
`history`, `tag`, `rm` and `prune` sub-pages) and the https://docs.docker.com/engine/release-notes/29/[Engine 29
release notes] for the new `image ls` view.

`docker image history` is the quickest way to see why an image is large: each row shows the instruction that
produced a layer and its size. Build steps that only change metadata (`ENV`, `LABEL`, `CMD`) appear with `0B`.

=== Moving images without a registry: `save` and `load`

[source,shell]
----
docker image save -o orders-1.4.0.tar myorg/orders-service:1.4.0    # layers + config + tags, as a tarball
scp orders-1.4.0.tar airgapped-host:
ssh airgapped-host docker image load -i orders-1.4.0.tar
----

Derived from https://docs.docker.com/reference/cli/docker/image/save/[`docker image save`] and
https://docs.docker.com/reference/cli/docker/image/load/[`docker image load`]. This is the documented way to move
images into air-gapped environments; it preserves every layer and the history.

=== `export`/`import` and `commit` -- and why not

* `docker container export` flattens a container's filesystem into a single tar and discards the history,
  configuration and layer sharing; `docker image import` turns it back into a one-layer image. Useful for
  forensics, not for shipping software.
* `docker container commit` snapshots a container's writable layer into a new image. _Learning Docker_ (chapter 2)
  uses it to create images interactively. The result is an image nobody can rebuild: there is no Dockerfile
  recording how it was made. Write a Dockerfile instead (see
  xref:backend/docker/dockerfile-fundamentals.adoc[Dockerfile Fundamentals]); keep `commit` for capturing a broken
  container's state for later debugging.

Derived from https://docs.docker.com/reference/cli/docker/container/commit/[`docker container commit`] and
https://docs.docker.com/reference/cli/docker/container/export/[`docker container export`].

== Labels and annotations

*Labels* are key-value metadata stored in the image configuration (set with `LABEL` in a Dockerfile or `--label`
at build time) and are visible to anything that inspects the image or its containers. Use the pre-defined
`org.opencontainers.image.*` keys -- `source`, `revision`, `version`, `created`, `licenses`, `title` -- so tooling
such as GHCR and Docker Scout can link an image back to its repository and commit.

*Annotations* are OCI metadata attached to manifests and indexes rather than to the image configuration. Registries
read them without downloading the config (GHCR shows `org.opencontainers.image.description` from an annotation, for
example), and they can differ per platform manifest.

[source,shell]
----
# Labels live in the image config and survive a local load
docker build --label org.opencontainers.image.source=https://github.com/myorg/orders-service \
  -t myorg/orders-service:1.4.0 .
docker image inspect --format '{{json .Config.Labels}}' myorg/orders-service:1.4.0

# Annotations live on manifests/indexes: the local image store cannot load them, so push directly
docker build --push --annotation "org.opencontainers.image.description=Orders service" \
  -t registry.example.com/myorg/orders-service:1.4.0 .
docker buildx imagetools inspect --raw registry.example.com/myorg/orders-service:1.4.0
----

Derived from https://docs.docker.com/build/metadata/annotations/[Annotations] (which notes that the Docker Engine
image store does not support loading images with annotations) and the
https://github.com/opencontainers/image-spec/blob/main/annotations.md[OCI pre-defined annotation keys].
`docker/metadata-action` generates both from Git metadata in CI (see
xref:backend/docker/ci-cd-with-github-actions.adoc[Docker CI/CD with GitHub Actions]).

== References

* https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-an-image/[Docker Docs -- What is an image?]
* https://docs.docker.com/get-started/docker-concepts/building-images/understanding-image-layers/[Docker Docs --
  Understanding the image layers]
* https://docs.docker.com/reference/cli/docker/image/[Docker Docs -- `docker image` CLI reference]
* https://docs.docker.com/reference/cli/docker/image/pull/[Docker Docs -- `docker image pull`]
* https://docs.docker.com/reference/cli/docker/image/save/[Docker Docs -- `docker image save`]
* https://docs.docker.com/reference/cli/docker/image/load/[Docker Docs -- `docker image load`]
* https://docs.docker.com/reference/cli/docker/container/commit/[Docker Docs -- `docker container commit`]
* https://docs.docker.com/reference/cli/docker/container/export/[Docker Docs -- `docker container export`]
* https://docs.docker.com/reference/cli/docker/container/diff/[Docker Docs -- `docker container diff`]
* https://docs.docker.com/reference/cli/docker/buildx/imagetools/inspect/[Docker Docs -- `docker buildx imagetools
  inspect`]
* https://docs.docker.com/build/building/multi-platform/[Docker Docs -- Multi-platform builds]
* https://docs.docker.com/build/metadata/annotations/[Docker Docs -- Annotations]
* https://docs.docker.com/engine/release-notes/29/[Docker Docs -- Docker Engine 29 release notes]
* https://github.com/opencontainers/image-spec[OCI Image Format Specification]
